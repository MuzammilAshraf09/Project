@model Project.Models.Entities.Order

@{
    ViewData["Title"] = "Delete Order";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #212121;
            color: #fff;
        }

        .card {
            background-color: #444;
            border: 1px solid #555;
        }

        .card-title, .card-footer {
            color: #fff;
        }

        .section-title {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: #fff;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="container-fluid bg-dark text-center py-4">
        <div class="d-flex justify-content-between align-items-center">
            <h1 class="display-5">Delete Order</h1>
        </div>
    </header>

    <!-- Delete Order Section -->
    <section class="py-5">
        <div class="container">
            <h2 class="section-title">Delete Order</h2>
            <div class="card mb-4">
                <div class="card-body">
                    <h5 class="card-title">Are you sure you want to delete Order ID: 1?</h5>
                    <form action="/Order/Delete" method="post">
                        <input type="hidden" name="Id" value="@Model.Id">
                        <button type="submit" class="btn btn-danger">Delete</button>
                        <a href="/Order/Index" class="btn btn-secondary">Cancel</a>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
