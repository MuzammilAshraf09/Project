<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Store Not Found</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #212121;
            color: #fff;
        }

        .navbar, .footer {
            background-color: #333;
        }

        .navbar-brand, .nav-link {
            color: #fff;
        }

            .navbar-brand:hover, .nav-link:hover {
                color: #c00;
            }

        .btn-primary {
            background-color: #f00;
            border: none;
        }

            .btn-primary:hover {
                background-color: #c00;
            }

        .card {
            background-color: #444;
            border: 1px solid #555;
        }

        .card-title, .card-text, .card-footer, .footer a {
            color: #fff;
        }

        .footer {
            padding: 20px 0;
        }

            .footer a:hover {
                color: #c00;
            }

        .section-title {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: #fff;
        }

        .banner-wrapper {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 400px; /* Set the height for centering */
            overflow: hidden;
            margin: 0 auto; /* Center the wrapper itself */
        }

        .banner-image {
            width: 100%;
            max-width: 900px;
            height: 400px;
            object-fit: cover;
            border-radius: 20px;
        }

        .card-img-top {
            width: 355px; /* Set the width */
            height: 230px; /* Set the height */
            object-fit: cover; /* Ensures the image covers the area without stretching */
            border-radius: 5px; /* Optional: rounded corners */
        }
    </style>
</head>
<body>
    <div class="not-found-container">
        <h1>Store Not Found</h1>
        <p>Sorry, the store you're looking for does not exist or is currently unavailable.</p>
        <a href="/" class="btn btn-primary mt-3">Go Back to Home</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>